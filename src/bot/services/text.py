"""
Сервисный модуль для работы с текстами бота.

Здесь нет импортов из aiogram — только «чистая» логика.
Его легко тестировать отдельно от Telegram.
"""


def build_start_message() -> str:
    """
    Формирует текст приветственного сообщения для команды /start.

    Функция ничего не знает о Telegram, она просто возвращает строку.
    """
    return (
        "Привет! 👋 Я простой эхо-бот.\n"
        "Напиши мне что-нибудь — и я повторю это сообщение."
    )


def build_help_message() -> str:
    """
    Формирует текст справочного сообщения для команды /help.
    """
    return (
        "Список доступных команд:\n"
        "/start — приветствие и краткое описание бота\n"
        "/help — показать это справочное сообщение\n\n"
        "Также бот повторяет любое обычное текстовое сообщение (эхо)."
    )


def build_echo_reply(user_text: str) -> str:
    """
    Формирует ответ для эхо-сообщения.

    Сейчас это просто тот же текст пользователя, но здесь
    можно добавить любую «умную» обработку: фильтры, форматирование,
    обращение к моделям и т.п.
    """
    return user_text


def build_chatgpt_message() -> str:
    """
    Формирует текст для команды /chatgpt.

    Пока здесь простое сообщение-заглушка, чтобы команда работала,
    а в будущем можно будет заменить на реальную логику.
    """
    return (
        "Команда /chatgpt пока не реализована.\n"
        "В будущем здесь можно будет общаться с ChatGPT."
    )


def build_image_message() -> str:
    """
    Формирует текст для команды /image.

    Аналогично, сейчас это заглушка до реализации реальной логики.
    """
    return (
        "Команда /image пока не реализована.\n"
        "В будущем здесь можно будет генерировать изображения."
    )


def build_history_message() -> str:
    """
    Формирует текст для команды /history.
    """
    return (
        "Команда /history пока не реализована.\n"
        "В будущем здесь будет список последних запросов."
    )
